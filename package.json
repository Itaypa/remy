{
  "name": "remy",
  "private": true,
  "version": "0.3.4",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "bun scripts/build-plugin.ts",
    "typecheck": "bunx tsc --noEmit",
    "test": "bun test",
    "preflight": "bun scripts/preflight.ts",
    "mutate": "bun scripts/mutation.ts",
    "adapt:smoke": "bun scripts/adapt-smoke.ts"
  },
  "devDependencies": {
    "@types/bun": "^1.2.23",
    "typescript": "^5.9.0"
  },
  "license": "Apache-2.0"
}
