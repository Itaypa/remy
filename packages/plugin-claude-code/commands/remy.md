---
description: 🐭 Session coaching report — waste found, tokens saved, next tip
allowed-tools: Bash
---

Run this exact command with the Bash tool:

```
"${CLAUDE_PLUGIN_ROOT}/bin/remy" report
```

Then display its complete output verbatim in a fenced code block — do not summarize, reformat, or omit any lines. After the code block, add at most two sentences highlighting the single biggest savings opportunity in the report. Do not read any other files or run any other commands.
