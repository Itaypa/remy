---
description: 🗓 7-day coaching rollup — tokens, cost, waste caught
allowed-tools: Bash
---

Run this exact command with the Bash tool:

```
"${CLAUDE_PLUGIN_ROOT}/bin/remy" report --week
```

Then display its complete output verbatim in a fenced code block — do not summarize, reformat, or omit any lines. After the code block, add at most one sentence noting the week's most interesting trend. Do not read any other files or run any other commands.
