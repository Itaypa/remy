---
description: 💤 Snooze the active coaching tip for 30 days
argument-hint: "[tip-id]"
allowed-tools: Bash
---

Run this exact command with the Bash tool (append the tip id argument only if one was provided):

```
"${CLAUDE_PLUGIN_ROOT}/bin/remy" dismiss $ARGUMENTS
```

Then display its complete output verbatim. Do not run any other commands.
